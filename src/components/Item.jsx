import styled from "@mui/system/styled";

const Item = styled("div")(() => ({
  // padding: "10vw",
  borderRadius: "2vw",
  // textAlign: "center",
  aspectRatio: "1/1.25",
  backgroundColor: "rgb(32, 32, 32)",
}));

export default Item;
